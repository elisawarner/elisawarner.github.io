domain: elisawarner.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https:/elisawarner.github.io  # the base hostname and protocol for your site, e.g. http://example.com

remote_theme: vaibhavvikas/jekyll-theme-minimalistic
plugins:
  - jekyll-remote-theme

github:
  is_project_page: false

title: Elisa Warner
description: Austin, TX USA